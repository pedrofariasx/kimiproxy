version: '3.8'

services:
  kimiproxy:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file:
      - .env
    volumes:
      - ./kimi_profile:/app/kimi_profile
    restart: always